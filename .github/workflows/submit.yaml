name: Manual submit
on: 
  workflow_dispatch:
    inputs:
      version:
        description: Path to dataset
        default: gs://some-bucket/some-dataset.tar.gz
        required: true
jobs:
  bump-dataset:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Example variable usage
      run: echo "The latest version is ${{ github.event.inputs.version }}"
    - name: Submit the main script
      id: submit-script
      run: echo '::set-output name=SELECTED_COLOR::green'
    - name: Get color
      run: echo "The selected color is ${{ steps.submit-script.outputs.SELECTED_COLOR }}"
